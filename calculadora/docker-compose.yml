services:
  calculadora:
    build: .
    container_name: paracarp-calculadora
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8001"
    volumes:
      - ./data:/data
    environment:
      - PORT=8001
      - DB_PATH=/data/pedidos.db
